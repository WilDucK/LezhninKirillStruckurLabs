# Лабораторная работа 5
## Простые деревья

### Выполнил: Лежнин Кирилл, группа ИДБ-25-07

### Базовая задача: Задача 3 - Обход бинарного дерева
### Обязательная часть: Реализовать функции:

- preorder(root), inorder(root), postorder(root): прямой, симметричный и обратный обходы, возвращающие список значений.

- level_order(root): обход в ширину (BFS), выводящий элементы по уровням.
  
- Создать несколько тестовых деревьев для проверки работы функций.

### Вариативная часть:

 - Посчитать количество путей от корня до листьев.

### Основная работа программы:

- Создание тестовых бинарных деревьев

- Выполнение прямого обхода 

- Выполнение симметричного обхода 

- Выполнение обратного обхода

- Выполнение обхода в ширину

- Подсчёт количества путей от корня до листьев

## Реализация

### Класс узла бинарного дерева
```python
class TreeNode:
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None
```
### Прямой обход (Preorder)

```python
def preorder(root, result=None):
    if result is None:
        result = []
    if root:
        result.append(root.value)
        preorder(root.left, result)
        preorder(root.right, result)
    return result
```

### Симметричный обход

```python
def inorder(root, result=None):
    if result is None:
        result = []
    if root:
        inorder(root.left, result)
        result.append(root.value)
        inorder(root.right, result)
    return result
```

### Обратный обход

```python
def postorder(root, result=None):
    if result is None:
        result = []
    if root:
        postorder(root.left, result)
        postorder(root.right, result)
        result.append(root.value)
    return result
```

### Обход в ширину (BFS) с использованием очереди

```python
from collections import deque

def level_order(root):
    result = []
    if root is None:
        return result
    
    queue = deque([root])
    while queue:
        node = queue.popleft()
        result.append(node.value)
        if node.left:
            queue.append(node.left)
        if node.right:
            queue.append(node.right)
    return result
```

### Подсчёт количества путей от корня до листьев

```python
def count_paths_to_leaves(root):
    if root is None:
        return 0
    if root.left is None and root.right is None:
        return 1
    return count_paths_to_leaves(root.left) + count_paths_to_leaves(root.right)
```
## Тестовые деревья
### Дерево 1: Разнообразное

```python
        10
       /  \
      5    15
     / \     \
    3   7     20
```

### Дерево 2: Только левые потомки

```python
    1
   /
  2
 /
3
```
### Дерево 3: Один узел

```python
return TreeNode(67)
```


### Дерево 4: Полное дерево высоты 2

```python
        1
       / \
      2   3
     / \ / \
    4  5 6  7
```

